# Backend (Express + Mongoose)
- Endpoints:
  - POST /api/auth/signup
  - POST /api/auth/login
  - GET /api/profile (protected)
  - CRUD: /api/items
- Uses JWT in Authorization header as `Bearer <token>`.
